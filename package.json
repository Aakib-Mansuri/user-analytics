{
  "name": "user-analytics",
  "version": "1.0.0",
  "description": "Full-stack user analytics application",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix demo",
    "dev": "concurrently --names \"BACKEND,DASHBOARD,DEMO\" --prefix-colors \"cyan,magenta,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix demo\"",
    "build": "npm run build --prefix frontend && npm run build --prefix demo"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
